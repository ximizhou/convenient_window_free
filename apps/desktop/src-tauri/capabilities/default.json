{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimum desktop permissions for configuration dialogs and public repository links.",
  "windows": ["main"],
  "platforms": ["windows"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save",
    {
      "identifier": "opener:allow-open-url",
      "allow": [
        {
          "url": "https://github.com/ximizhou/convenient_window_free"
        }
      ]
    }
  ]
}
